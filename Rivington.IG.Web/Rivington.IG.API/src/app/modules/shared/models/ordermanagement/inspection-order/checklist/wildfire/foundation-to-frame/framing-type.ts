import { Enumeration } from "../../../../../../enumeration";

export class FramingType implements Enumeration {
    public constructor(init?:Partial<Enumeration>) {
        Object.assign(this, init);
    }
}